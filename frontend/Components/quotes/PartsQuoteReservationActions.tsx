"use client";
import {useState} from "react";import {useRouter} from "next/navigation";
export default function PartsQuoteReservationActions({quoteId,branchId,reserved=false}:{quoteId:string;branchId?:string|null;reserved?:boolean}){
 const router=useRouter();const[busy,setBusy]=useState("");const[error,setError]=useState("");
 async function act(action:string){setBusy(action);setError("");try{const r=await fetch("/api/quotes/parts-reservation",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action,quoteId,branchId})});const b=await r.json();if(!r.ok)throw new Error(b.error||"Unable to update Parts quote.");if(action==="invoice"&&b.invoice_id){router.push(`/invoices/${b.invoice_id}`);return;}router.refresh();}catch(e){setError(e instanceof Error?e.message:"Unable to update Parts quote.");}finally{setBusy("")}}
 return <div className="space-y-3"><div className="flex flex-wrap gap-2">{!reserved?<button disabled={!!busy} onClick={()=>act("reserve")} className="rounded-xl bg-[var(--brand-strong)] px-4 py-2.5 text-sm font-bold text-white disabled:opacity-50">{busy==="reserve"?"Reserving…":"Reserve stock"}</button>:<><button disabled={!!busy} onClick={()=>act("invoice")} className="rounded-xl bg-[var(--brand-strong)] px-4 py-2.5 text-sm font-bold text-white disabled:opacity-50">{busy==="invoice"?"Creating invoice…":"Accept & create Parts invoice"}</button><button disabled={!!busy} onClick={()=>act("release")} className="rounded-xl border px-4 py-2.5 text-sm font-bold">Release reservation</button></>}<button disabled={!!busy} onClick={()=>act("decline")} className="rounded-xl border border-red-200 px-4 py-2.5 text-sm font-bold text-red-700">Decline quote</button></div>{error?<p className="text-sm font-semibold text-red-600">{error}</p>:null}</div>;}